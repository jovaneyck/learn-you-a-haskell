Running haskell from REPL
===

* Open a REPL:

```[bash]
stack ghci
```

* Load a script filename.hs:

```[haskell]
Prelude> :l filename
```

* Reload on changes

```[haskell]
Prelude> :r
```

* Exit ghci

```[haskell]
Prelude> :q
```